<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.virtualtourar">

  <!-- Core permissions -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />

  <!-- Location (foreground + background for geofences) -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <!-- Android 10+ background location (needed for geofences in background) -->
  <uses-permission
      android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
      tools:targetApi="q" />

  <!-- Boot so we can re-register geofences after restart -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <!-- (Optional) Only if you actually use them elsewhere -->
  <!-- <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" /> -->
  <!-- <uses-permission android:name="android.permission.BODY_SENSORS" /> -->
  <!-- <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" /> -->

  <!-- Required device features -->
  <uses-feature android:name="android.hardware.camera" android:required="true" />
  <uses-feature android:name="android.hardware.camera.ar" android:required="true" />
  <uses-feature android:glEsVersion="0x00030000" android:required="true" />

  <application
      android:appCategory="game"
      android:allowBackup="false"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="false"
      tools:ignore="GoogleAppIndexingWarning">

    <!-- ARCore -->
    <meta-data
        android:name="com.google.android.ar.API_KEY"
        android:value="@string/arcore_api_key" />
    <meta-data
        android:name="com.google.ar.core"
        android:value="required" />

    <!-- Receiver used by GeofencingClient PendingIntent.
         No intent-filter; keep exported=false. -->
    <receiver
        android:name=".geofence.GeofenceBroadcastReceiver"
        android:exported="false" />

    <!-- Re-register geofences after device reboot or app update.
         Needs exported=true to receive system broadcasts. -->
    <receiver
        android:name=".geofence.BootCompletedReceiver"
        android:exported="true"
        android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <!-- Optional: receive earlier, before user unlocks -->
        <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
        <!-- Re-register after app update -->
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <!-- Launcher -->
    <activity
        android:name=".HomeActivity"
        android:theme="@style/AppTheme"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Other screens -->
    <activity android:name=".GeospatialActivity" />
    <activity android:name=".CollectiblesActivity" />

  </application>
</manifest>
